import colours from '../../styles/colours';

export const defaultCheckboxStyle = {
  unCheckedColour: colours.PRIMARY.NORMAL,
  checkedColour: colours.PRIMARY.NORMAL,
  disabled: {
    unCheckedColour: colours.PRIMARY.LIGHTER,
    checkedColour: colours.PRIMARY.LIGHTER,
  },
};

export default defaultCheckboxStyle;
